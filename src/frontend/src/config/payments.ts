// Centralized payments bypass configuration
// This flag can be used to temporarily disable payments for testing or maintenance
// When false (default), payments are controlled by Stripe configuration status
export const PAYMENTS_BYPASS_ENABLED = false;
